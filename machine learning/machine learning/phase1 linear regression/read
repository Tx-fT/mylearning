  模板结构总览

  文件分 8 个模块，跟你学的内容一一对应：

  ┌───────────────────────┬──────────────────────────────────────────────────────┐
  │         模块          │                     对应你的笔记                     │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 1. 数据生成           │ training set (x, y)                                  │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 2. predict()          │ 模型 f_w,b(x) = wx + b                               │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 3. compute_cost()     │ 代价函数 J(w,b) = 1/(2m) * Σ(ŷ - y)²                 │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 4. compute_gradient() │ ∂J/∂w = (1/m) * Σ(f-y) * x 和 ∂J/∂b = (1/m) * Σ(f-y) │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 5. gradient_descent() │ w = w - α * ∂J/∂w, b = b - α * ∂J/∂b（同时更新）     │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 6. evaluate()         │ MSE / MAE / R² 评估指标                              │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 7. plot_results()     │ 散点 + 拟合直线 + cost 收敛曲线                      │
  ├───────────────────────┼──────────────────────────────────────────────────────┤
  │ 8. experiment()       │ 自由实验区                                           │
  └───────────────────────┴──────────────────────────────────────────────────────┘

  ---
  建议的实验方向

  修改顶部 CONFIG 区参数，重跑看变化：

  - LEARNING_RATE — 试试 0.0001（收敛太慢）vs 1.0（发散炸掉），体会你笔记里 "control step" 的含义
  - NOISE_STD — 试试 0（完美拟合）vs 10（噪声大，R² 降低）
  - INIT_W / INIT_B — 换不同起点，验证凸函数总是收敛到同一个最小值（笔记里的 "bowl-shaped → single minimum"）

  experiment() 函数是你自己写练习代码的地方，直接在里面加逻辑即可。
  是 NumPy 数组（numpy.ndarray）。可以验证一下：

  import numpy as np

  x = np.random.uniform(-10, 10, 100)

  print(type(x))   # <class 'numpy.ndarray'>
  print(x.shape)   # (100,)  —— 100 个元素的一维数组
  print(x[:5])     # 打印前 5 个看看

  列表 vs NumPy 数组

  ┌──────────┬───────────────────────────────────┬─────────────────────────────────┐
  │          │         Python 列表 list          │       NumPy 数组 ndarray        │
  ├──────────┼───────────────────────────────────┼─────────────────────────────────┤
  │ 创建     │ [1, 2, 3]                         │ np.array([1, 2, 3])             │
  ├──────────┼───────────────────────────────────┼─────────────────────────────────┤
  │ 乘法     │ [1,2] * 3 → [1,2,1,2,1,2]（重复） │ arr * 3 → [3,6,9]（逐元素乘法） │
  ├──────────┼───────────────────────────────────┼─────────────────────────────────┤
  │ 向量运算 │ 不支持，要写循环                  │ w * x + b 一行搞定              │
  └──────────┴───────────────────────────────────┴─────────────────────────────────┘

  这也就是为什么模板里可以直接写：

  predictions = w * x + b    # x 是 ndarray，所以整批数据一次算完

  如果 x 是普通列表，w * x 会变成重复而不是逐元素乘法，算出来就错了。

  快速转换

  # 列表 → 数组
  my_list = [1, 2, 3]
  arr = np.array(my_list)

  # 数组 → 列表
  my_list_again = arr.tolist()

  总结：模板里的 x 和 y 都是 shape 为 (100,) 的一维 ndarray，不是列表。这也是为什么能直接做向量化运算的原因。